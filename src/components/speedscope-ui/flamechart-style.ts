// Removed Aphrodite imports and StyleSheet.create
// import {StyleSheet} from 'aphrodite';
import {FontSize, Sizes} from './style'; // Keep Size/Font constants if needed elsewhere
// import {withTheme} from './themes/theme'; // Removed withTheme

// The getFlamechartStyle function is removed as styles will be applied
// directly using Tailwind classes in the consuming components.
// If specific style logic calculations were done here, they might need
// to be moved or adapted.

// Exporting constants if they are still needed
export { FontSize, Sizes };
